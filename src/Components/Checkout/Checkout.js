const Checkout = (props) => {
    return (
        <div style={{ height: "71vh" }}>
            <h3>{props.name}</h3>
            <h1>Checkout Service</h1>
        </div>
    );
};

export default Checkout;
